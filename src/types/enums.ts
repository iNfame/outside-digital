export enum EventType {
	NOT_READY = 'not_ready',
	READY = 'ready',
	ACTIVE = 'active',
	RUNNING = 'running',
	PAUSED = 'paused',
	IDLE = 'idle',
	ERROR = 'error',
	VEHICLE_ERROR = 'vehicle_error',
	OFFLINE = 'offline',
}
